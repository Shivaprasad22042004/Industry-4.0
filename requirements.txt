# Phase 1: Physics Engine (no network dependencies)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Phase 2: FastAPI + WebSocket
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# Phase 4: TimescaleDB
asyncpg>=0.29.0
